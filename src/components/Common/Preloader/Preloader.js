import preloader from '../../../assets/preloader.gif'

const Preloader = (props) => {
    return (
        <div>
            <img src={preloader} alt="" height={100}/></div>
    )
}

export default Preloader